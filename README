
            
                                                        
                欢迎使用 UCDOS SDK for C/C++ 开发包!    
                                                        
               一流的 DOS 环境多窗口图形界面开发系统!   
                                                        
            

                        北京希望高技术集团·简晶

                            １９９６年１０月


    本文件描述了『UCDOS SDK for C/C++ 使用手册』印刷结束后新增的功能
及其相关函数。

    阅读本文件时，请同时参考『使用手册』。




 目录
================================
 SDK 安装目录结构
 SDK 开发包主要文件介绍
 SDK 窗口的基本操作
 新增加的函数说明
 编程要点指正




┏━━━━━━━━┓
┃SDK 安装目录结构┃
┗━━━━━━━━┛

    经过正常安装后，UCDOS SDK foe C/C++ 的目录结构如下图所示：

    CSDK─┐                      SDK 开发系统
          ├ EXAMPLES ┐          开发教学例子目录
          │          ├ SOURCE   SDK 的部分源程序(例子)
          │          ├ WIN      窗口的各种形式举例
          │          └ WID      各种构件举例
          │
          ├ APP  ──┐          应用实例目录
          │          ├ EDITOR   简易编辑器
          │          ├ GO       围棋双人游戏
          │          ├ LIFE     生命繁衍游戏
          │          ├ VIEWICO  Windows ICO 图标浏览器
          │          ├ VIEWBMP  Windows BMP 位图浏览器
          │          └ MOVIE    动画播放演示程序
          │
          ├ CPP                  以 C++ 实现的一些应用程序实例
          ├ BMP                  提供的一些 BMP 位图文件
          └ ICO                  提供的一些 ICO 图标文件

    除 SOURCE 目录外，例子目录中的每一个 .C 例子程序均可单独编译、执
行，已包含正确编译后的.EXE执行文件。这些执行文件在启动 UCDOS 5.0/6.0
后即可直接使用。
    需注意的是，如果使用 UCDOS 5.0，则先要将 <CSDK> 目录中的 SDK.DRV
文件拷贝到 <UCDOS\DRV> 目录中。

    如果需要重新编译上述例子程序，请根据您使用的 BC 版本，按照『使用
手册』的说明为其建立相应的工程文件。
    <APP> 中每个应用举例目录及 <CPP> 目录中都有更详细的相应说明文件
README.TXT, 不能错过。




┏━━━━━━━━━━━┓
┃SDK 开发包主要文件介绍┃
┗━━━━━━━━━━━┛


■ SDK.LIB / SDKNOR.LIB                                   开发连接库
──────────────────────────────────

    SDK.LIB 是经过优化编译的开发库，经它连接出来的 EXE 程序，比使用
未经优化编译的 SDKNOR.LIB 库少占用 10K 左右的内存。
    由于 C 编译器的无法控制，我们觉得同时配备这两个库比较好，如果使
用优化库 SDK.LIB 后程序出问题，那么请用 SDKNOR.LIB 替换 SDK.LIB。

    另外，使用 BC 的编译优化开关时, 不要选择 "Fastest Code" 模式, 这
种模式编译的 SDK 程序可能是有问题的; 但是可以选择 "Smallest Code" 模
式, 使应用程序尽量短小。


■ SDK.DRV                                                驱动程序库
──────────────────────────────────

    驱动程序库是任何 SDK 应用程序启动后必备的一个程序,  SDK 安装结束
后, 请将它拷贝到 UCDOS 5.0/6.0 的 <UCDOS\DRV> 目录中。
    SDK.DRV 所起的作用是辅助 UCDOS 完成 SDK 图形界面的管理。


■ MAKEHELP.EXE                                       帮助文件编译器
──────────────────────────────────

    1. 帮助系统介绍
    ---------------

    在 UCDOS SDK for C/C++ 中，提供了一个类似“Windows Help”的帮助系
统，它可以为应用程序提供联机帮助。MAKEHELP.EXE 用来编译该帮助系统的帮
助文件。
    在帮助系统中，帮助信息按一页页的方式存放，每一个页解释一个帮助主
题；页与页之间可以相互引用、跳转。除了页以外，还可支持弹出式的名词解
释，这个被解释的名词称为关键词。帮助窗口出现后，信息显示情况如下:

    . 正常文本     : 黑色文字
    . 跳转的主题   : 绿色带下划实线显示的字符
    . 弹出的关键词 : 绿色带下划虚线显示的字符

    可以使用 TAB / SHIFT+TAB 来选择主题或关键词，按回车后可跳到新的
主题页或弹出名词解释小窗口。也可使用鼠标单击这些绿色的字符，起到相同
作用。
    在编写帮助文件时，可以使用任何一种编辑器，最后通过 MAKEHELP 程序
将其编译成为后缀名为“.USH”的 SDK 帮助文件，就可以直接在 SDK 应用程
序中使用了。使用后面介绍的 UC_WinHelp 函数，即可启动帮助系统。

    2. MAKEHELP 的使用方法
    ----------------------

    MAKEHELP 程序的使用格式为：

    MAKEHELP filename

    之后将生成一个同名，但后缀名为.USH的文件。默认的情况下，filename
后缀名是“.TXT”。如果帮助文件由多篇文本组成，也可以建立一个编译工程
文件，其中只需写上所有的文本文件名，filename 指定工程文件即可。工程
文件的后缀名必需是“.HPJ”。

    3. 帮助文本的编写规则
    ---------------------

    帮助文本可由任何一个文本编辑器来编写。源文件规定每行长度小于 136
个字符。在文件中的 TAB 字符被转换为一个空格符。文本中至少要有一个主题
描述。下面介绍文本中的编译控制符:

    主题名称:   以“$C[”开始，“]”结束，且单独占一行。
                从下一行开始到下一主题名称或关键词名称的上一行之间的
                内容，是本主题页的帮助文本。
    关键词名称: 以“$K[”开始，“]”结束，且单独占一行。
                从下一行开始到下一主题名称或关键词名称的上一行之间的
                内容，是本关键词的解释文本。
    主题引用:   在正常文件中，以“$C”开始，“$E”结束之间写上需要引
                用的主题名称。
    关键词引用: 在正常文件中，以“$K”开始，“$E”结束之间写上需要引
                用的主题名称。

    如果文本中需要显示“$”字符，则以“$$”替代即可。

    下面很短的例子可充分说明帮助文件的制作是很简单的:

    ─────────────────────────
     $C[太阳]
     太阳是一颗恒星, 它有一颗卫星是$C地球$E.
     $C[地球]
     地球是$C太阳$E的一颗行星, 它有一颗卫星是$K月亮$E.
     $K[月亮]
     月亮是地球的一颗卫星.
    ─────────────────────────

    这个例子也存放在 <CSDK> 目录下, 文件名是 DEMO.TXT, 您可以键入命令
MAKEHELP demo 加回车, 即可编译出 DEMO.USH 帮助文件, 进入 UCSHELL 的帮
助窗口打开 DEMO.USH, 将看到有关 "太阳/地球/月亮" 的相互关系。


■ TESTADP.EXE                                        显示卡探测程序
──────────────────────────────────

    报告显示卡类型及 VESA 信息。如果 UCDOS 或者 SDK 应用程序对某种显
示卡的支持出现问题，可以使用 TESTADP 检测显示卡，自己若分析不出原因，
可将检测报告寄给我们。检测报告的制作方法如下：

    1. 联接好打印机
    2. 执行  TESTADP > report.txt
    3. 执行  TYPE report.txt > prn

    除将报告打印出来外，如果您有电话及 Modem，也可通过 UCCOM 通讯员将
report.txt 发传真或传送到希望公司。




┏━━━━━━━━━┓
┃SDK 窗口的基本操作┃
┗━━━━━━━━━┛

    从前的 DOS 应用程序界面和操作方千变万化，每接触一个应用，就需要学
习掌握一套不同的程序操作方法，往往让操作人员无所适从。但如果使用 SDK
编写 DOS 应用程序，情况将大大改善。
    与 Windows 操作系统类似，SDK 编写应用程序有着共同的界面和操作方法，
从 UCDOS 已经占据全国中文 DOS 平台绝大部分市场的情况来看，使用 SDK 编
写您的应用程序将具有明显的优势：

    . 有广泛普及使用的中文操作平台作为支持
    . SDK for C/C++ 高性能的开发手段
    . 类 Windows 的统一界面和操作方法

    下面介绍 SDK 应用程序界面的统一基本操作方法:

1. 键盘命令

    CTRL+F4     关闭窗口
    CTRL+F5     移动窗口     : 虚框出现后按下上下左右键头可移动窗口
                修改窗口尺寸 : 虚框出现后，按住 SHIFT 键不放，使用上
                               下左右键头可修改窗口尺寸
    CTRL+TAB    窗口之间相互切换
    ENTER       当前窗口是图标时，放大窗口
    TAB         在当前窗口中切换操作焦点
    ESC         关闭对话框
    F10, ALT    激活 / 关闭菜单

2. 鼠标命令

   关闭窗口           : 单击窗口左上角“×”形按钮
   窗口极大化为全屏幕 : 单击窗口右上角“∧”形按钮 或者
                        双击窗口标题
   窗口极小化为图标   : 单击窗口右上角“∨”形按钮
   图标窗口放大       : 双击图标
   移动窗口           : 按住窗口标题不放, 将窗口拖动到新的位置上
   修改窗口尺寸       : 拉伸窗口四周或四角的边框线




┏━━━━━━━━┓
┃新增加的函数说明┃
┗━━━━━━━━┛

    以下是『使用手册』印刷好后, 新增加的函数及其说明, 这里按照字母顺
序对函数进行排列。

■ 单线窗口风格

   窗口定义(UC_DefineWindow)的风格增加了一个单线属性:

   WS_SINGLELINE

   具有这种属性的窗口边框显示为单线，窗口没有尺寸变化功能，如果作为
含 WS_MAIN 属性的主窗口，窗口右上角将有一个极小化按钮；但如果含有属
性 WS_NOSIZE 或者 WS_NOMOVE，那么极小化按钮会消失。


■ void UC_ArrangeIcons(void);

   功能: 重新排列屏幕上所有的窗口极小化图标


■ void UC_CascadeWindows(int left, int top, int right, int bottom);

   功能: 重新对屏幕上的所有打开窗口在指定区域内层叠排列, 使每个窗口的
         标题栏可见
   参数: left, top, right, bottom 指定了排列区域的左上角/右下角绝对坐标


■ int UC_CheckEditPoint(char *buf, int curepos);

   功能: 检取一个字串的指定位置处是否汉字
   参数: buf     = 字串指针(字串以 NULL 结束)
         curepos = 指定的位置
   返回: 直接返回下列标识符表示的字串指定位置状态:

         STR_NULL       是NULL字符(或超出字串长度)
         STR_HZLEFT     是汉字左半部分
         STR_HZRIGHT    是汉字右半部分
         STR_ASCII      是英文字符

   说明: 本函数在汉字字串的编辑过程中可起到重要作用.


■ int UC_CheckKeyboard(void);

   功能: 检测当前的键盘按键信息, 并调用用户的键盘事件处理接口
   返回: NULL = 没有键盘消息
         否则表示有键盘消息, 直接返回当前的键盘键盘码


■ void UC_DialogAbout(void (*funok)(void), void (*ico)(), int icn,
                       char *title, char *fmt,...);

   功能: 显示一 About 对话框
   参数: funok  =  按确定按钮后的回调函数
         ico    =  About 对话框中显示的图标数据指针
         icn    =  图标计数(一般为1)
         title  =  自定义对话框标题字串
         fmt    =  显示内容(用 \n 可换行)
   说明: 该 About 窗口以标准的形式显示应用程序的版权信息等内容, 同时
         显示出系统当前的一些状态信息。


■ void UC_DisplayTitle(WINDOWS *wnd);

   功能: 重新刷新显示窗口标题
   参数: wnd = 窗口句柄
   说明: 可在窗口标题动态更换后, 使用本函数.


■ int UC_GetColourDepth(void);

   功能: 检取当前屏幕的颜色深度
   返回: 以下列标识符表示的当前屏幕支持的颜色级别:

         CRD_MONO          单色模式(MONO/HGC)
         CRD_COLOR16       EGA/VGA/SVGA 16色
         CRD_COLOR256      VGA/SVGA 256色
         CRD_HICOLOR15B    SVGA 15位真彩色(32K)
         CRD_HICOLOR16B    SVGA 16位真彩色(64K)
         CRD_TURECOLOR24B  SVGA 24位真彩色(16.8M)
         CRD_TURECOLOR32B  SVGA 32位真彩色(16.8M+A)


■ int UC_GetDiskAttribute(int drv);

   功能: 检取指定驱动器的属性
   参数: drv = 驱动器代号( 0=A:, 1=B:, 2=C: ... )
   返回: NULL 表示驱动器不存在;
         否则返回下列标识符代表的驱动器类型:

         DRV_360K       360K 5寸大软驱
         DRV_12M        1.2M 5寸大软驱
         DRV_720K       720K 3寸小软驱
         DRV_144M       1.44M 3寸小软驱
         DRV_HARD       硬盘(含网络及各种映射盘符)
         DRV_CDROM      光驱


■ int UC_GetTextAppositeWidth(RECT *rect, WORD format, char *fmt,...);

   功能: 以当前字型检取多行字串在一个矩形中自动分行后的最合适宽度
   参数: rect   = 矩形范围
         format = 与 UC_DrawText 函数一致的字串显示格式
         fmt    = 变量字串
   返回: 实际最合适的宽度. 此宽度值不会超出 rect 所给出的宽度范围.


■ void UC_HTileWindows(int left, int top, int right, int bottom);

   功能: 重新对屏幕上的所有打开窗口在指定区域内横向平铺排列, 使每个窗
         口全部可见
   参数: left, top, right, bottom 指定了排列区域的左上角/右下角绝对坐标


■ void UC_MouseBack(void);

   功能: 鼠标消息回退
   说明: 有时需要将本次鼠标消息回退, 让其他地方进行处理.


■ int UC_MouseCheck(void);

   功能: 从鼠标事件队列中取出当前鼠标消息
   返回: FALSE = 没有鼠标消息
         TURE  = 有鼠标消息, 该消息被存于全局结构 MOUSE_AGI 中
   说明: 有关 MOUSE_AGI 请参阅『使用手册』MOUSE 事件信息结构.


■ void UC_VTileWindows(int left, int top, int right, int bottom);

   功能: 重新对屏幕上的所有打开窗口在指定区域内纵向平铺排列, 使每个窗
         口全部可见
   参数: left, top, right, bottom 指定了排列区域的左上角/右下角绝对坐标


■ void UC_VGAFadeIn(int delay, char *pal);

   功能: 渐显指定的调色板
   参数: delay = 延时
         pal   = 显示的8位格式调色板(768字节, 一般是当前调色板数据)
   说明: 可使用 UC_ReadBiosPalette 函数读出当前的8位格式调色板数据.
         本函数及下一函数可实现屏幕淡入淡出的屏幕特技效果, 但仅支持在
         VGA 的 16/256 色模式下实现.


■ void UC_VGAFadeOut(int delay);

   功能: 渐黑当前的屏幕
   参数: delay = 延时


■ void UC_WinHelp(char *pcHelpTitle, char *pcFileName, char *pcKeyWord,
                   int iLeft, int iTop, int iWidth, int iHigh );

   功能: 显示帮助窗口
   参数: pcHelpTitle  = 帮助窗口标题
         pcHelpFile   = 帮助文件名及其路径
         pcKeyWord    = 第一屏跳转的帮助主题关键词(NULL=进入目录)
         iLeft,iTop,
         iWidth,iHigh = 窗口的左上角/右下角坐标(0, 0, 0, 0 为默认)
   说明: SDK 提供了类 Windows 的帮助系统, 可以主题形式一屏屏按照操作
         者需要跳转查询帮助信息.




┏━━━━━━┓
┃编程要点指正┃
┗━━━━━━┛

    这里给出一些我们内部使用 SDK编写程序时总结出来的一些容易混淆的基
本概念和理解要点，在实际的编程实践中, 您也将有更多的体验, 欢迎您总结
之后与我们多多交流, 使 SDK 的实际问题解决能力进一步提高。

1. 透彻理解窗口的三个重要事件回调函数

   Redraw 函数只应该单纯处理客户区内容的绘制，不要在此函数中做诸如改
变滚动条相对值、进行多余计算等工作。如果客户区的显示内容需要计算，那
么应该在 Redraw 之前就必需计算好。记住：Redraw 只显示，不做其他事情!

   Resize 函数只应该对窗口尺寸变化后，客户区的显示范围、滚动条的相对
值范围等需要的地方进行预算处理，而不能对屏幕进行任何操作。

   Close 函数如果不处理，那么该窗口将不能关闭。
   如果窗口只需关闭，则应该使用 UC_WindowClose 函数；如果窗口关闭后
要退出应用程序，则应该使用函数 UC_CloseUCVision。

2. 主动更新滚动条

    一般情况下，如果操作者按下滚动条按钮，滚动条将自动更新，并通知应
用程序。但是应用程序也可主动对滚动条相对值范围进行更新，然后更新屏幕。
如果:

    仅更新了当前值: 则使用 UC_UpdateScrollbar 函数
    更新了最大值: 则请使用 UC_DisplayScrollbar 函数

3. 不具有 WS_MAIN 属性的窗口是不能与其他窗口相互切换的。同样，如果一
   个不具有 WS_MAIN 属性的窗口弹出了一个具有 WS_MAIN 的窗口, 那么这个
   新窗口本来具有的窗口切换功能也将被自动取消。

4. 所有的 GUI 控制(UC_WindowsCentral)只对当前窗口有效。一个窗口被关闭
   后(UC_WindowClose)，其后面的第一个窗口将自动激活；如果其后已经没有
   窗口，则应用程序不应该选择窗口关闭，而应该是程序退出(UC_CloseUCVi-
   sion)，否则 SDK 系统将发出错误信息，自动中断程序的运行。

5. 为了及时发现应用程序中的错误，程序员应该尽量使用 SDK 提供的MyFREE
   函数替代 C语言提供的 free 函数。MyFREE 有强力的错误探测能力，系统
   运行中一旦出现不稳定状态，MyFREE 将终止程序，并提示“异常错误”，
   开发者因此可及时调试出错的部分，最终产生出质量较高的程序来。

6. 欲编写优质无错的 C语言程序，除尽量使用 MyFREE 外，灵活使用 SDK 提
   供的断言函数 UC_Assert 也很重要。将程序中一些值得怀疑的地方用断言
   标记上，这将辅助您弄清程序具体在搞了些什么，其流程又是如何变化。

7. 编写无错 C 语言程序的一个重要忠告就是：从编写程序第一行开始，就严
   格控制住程序中可能出现的任何情况，不要对程序运行作出假设或者默认。
   最简单的例子有:

   A. 假设文件打开成功,一旦出现不成功情况,后面的程序将造成未知损害!
   B. 假设内存分配成功,一旦出现不成功情况,其他内存将受到被破坏的威胁!

8. 处理列表框时, 要明白一点，SDK 并不主动显示列表框中的内容，而必需由
   应用程序自己通过 UC_UpdateListBox 函数显示，而且这一操作只能在列表
   框滚动条的回调函数中进行。

9. 如果需要重新刷新某一窗口，不应该使用 UC_WindowEnable 函数，而应该
   用 UC_RedrawWindow 函数。因为 UC_WindowEnable 仅仅激活窗口，除非
   第一次显示窗口，否则并不能起到更新窗口内容的作用。
